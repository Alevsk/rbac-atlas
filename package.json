{
  "name": "rbac-atlas",
  "version": "1.0.0",
  "description": "A curated database of identities and RBAC policies in Kubernetes projects",
  "scripts": {
    "fmt": "prettier --write 'content/**/*.md'",
    "lint": "prettier --check 'content/**/*.md'",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "prettier": "^3.5.3",
    "prettier-plugin-go-template": "^0.0.15"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
